# Gate 2：17.0°根闭合与空间收敛审计

日期：2026-08-26  
结论状态：**17.0°空间数值收敛通过；无冻结物理参考；二维先决门保持 FAIL，Gate 2保持17/20 PASS**

## 1. 目的与边界

本审计从17.5°冻结源继续推进到17.0°，检验参考隔离的0.5°斜升角同伦、主体流--薄射流根闭合、128模态形状修正以及三层空间重构是否仍保持稳定。求解过程只使用离散方程残差、根缺陷、条件数、位移和曲率约束，不读取自由面、压力、载荷或整船试验参考。

17.0°没有冻结权威物理参考，因此本结果只证明数值延拓和空间稳定资格，不能代替10°、20°、30°楔形入水物理验收。

## 2. 同伦与根闭合

从17.5°的160面板冻结源 `self_similar_wedge_17p5deg_n160_dct128_rootpreserve_fixedbranch_curvature_trust025_v454` 出发，以楔顶为中心施加向远场衰减的 `-0.5 deg` 局部旋转。尺度1.0候选保持匹配面零位移，初始 `K=6.269568e-2`、根部相对失配 `2.099566e-3`、条件数比 `0.999220`，且未使用参考数据。

32模态 `close` 首步将 `K` 降至 `5.391298e-2`。沿冻结方向继续计算时接受23步，最终 `K=5.462080e-3`、根部相对失配 `2.457315e-5`、条件数 `9.639601e4`。随后128模态 `preserve` 修正通过完整非线性候选检查，将160面板源的 `K` 降至 `4.133131e-3`，条件数为 `9.634499e4`。

## 3. 三层空间结果

从同一个160面板冻结源独立生成320、640和1280面板网格，统一使用256个射流边界积分面板且不增加伪时间迭代：

| 外自由面面板数 | 精确运动学积分 K | 根部相对失配 | 边界积分条件数 |
| ---: | ---: | ---: | ---: |
| 320 | `1.572520e-3` | `2.290689e-4` | `1.992836e5` |
| 640 | `9.880650e-4` | `3.052218e-4` | `4.461442e5` |
| 1280 | `8.851929e-4` | `3.396946e-4` | `1.072374e6` |

320面板层高于 `K<=1e-3`，640和1280面板均通过且继续下降。最细两层之间，偶极系数变化 `0.021989%`、积分垂向力变化 `0.000948%`、压力峰变化 `0.000982%`、自由面相对二范数变化 `0.649094%`、完整船体压力曲线变化 `0.002269%`。三层趋势、同源检查和空间稳定性机器判定均为 `PASS`。

诊断运动学积分在最细两层间仍变化 `10.411%`，但两层绝对值均已低于门槛；物理输出和几何量满足冻结的5%空间稳定要求。该差异被保留在机器清单中，没有选择性删除。

最细网格检查点经哈希校验、文件读取和边界元重建后的最大相对往返误差为 `6.591949e-17`。

## 4. 冻结证据

| 证据 | 路径或状态 |
| --- | --- |
| 17.0°同伦初值 | `outputs/self_similar_wedge_17deg_from17p5_n160_rotate_homotopy0_v459` |
| 32模态根闭合 | `outputs/self_similar_wedge_17deg_n160_dct32_rootclose_fixedbranch_curvature_trust025_v460` |
| 根闭合方向复用 | `outputs/self_similar_wedge_17deg_n160_dct32_rootclose_direction_reuse32_v461` |
| 128模态冻结源 | `outputs/self_similar_wedge_17deg_n160_dct128_rootpreserve_fixedbranch_curvature_trust025_v462` |
| 320/640/1280面板层级 | `outputs/self_similar_wedge_17deg_n320_jet256_dct128_regrid0_v463`、`v464`、`v465` |
| 空间收敛机器清单 | `outputs/self_similar_wedge_17deg_dct128_grid_convergence_v466/grid_convergence.json` |
| 空间数值状态 | `PASS` |
| 独立物理验证状态 | `FAIL` |

## 5. 结论与下一步

17.0°已形成参考隔离、根接口闭合、三层空间稳定且可无损重载的数值同伦源。它支持继续按0.5°步长向16.5°推进，但不改变二维先决门和Gate 2状态。

到达10°后，仍必须完成冻结参考自由面、压力、积分载荷、时间步、远域及独立路径复现验收；只有物理和数值条件全部通过，才能将10°子门改判为 `PASS`。

本轮未修改生产代码。最新完整软件回归仍为 `602 passed, 0 failed, 1247 warnings`；代码未变化，因此未重复运行完整回归。

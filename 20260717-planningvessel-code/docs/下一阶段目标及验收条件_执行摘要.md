# 下一阶段目标及验收条件：执行摘要

日期：2026-08-22  
上位目标：[高速船型运动预报系统总体目标](./高速船型运动预报系统总体目标.md)  
详细口径：[下一阶段阶段目标及可验收条件](./下一阶段阶段目标及验收条件_20260822_Gate2闭合.md)

## 当前判断

- Gate 1 已通过，Gate 2 仍为 `17/20 PASS`。
- 五频点直接波浪激励数值稳定，但整船响应未通过：升沉中位误差 `76.16%`，纵摇中位误差 `36.85%`，主峰频率误差约 `32%--34%`。
- Sun 2007 图 7.13 `NUM` 五频率、八系数数值复现基线已完成，源级验收为 `FAIL`。数值与趋势检查通过，但 `A35`、`A55`、`B53` 的中位相对误差分别为 `86.03%`、`131.51%`、`55.03%`，当前必须先闭合耦合附加质量和纵摇相关阻尼幅值，不能直接铺开三航速计算。该数据是同源数值复现基准，不是 Troesch 独立试验。

## 唯一阶段目标

**先通过 Sun 2007 `NUM` 五频率八系数数值复现门，再完成 Troesch 实验对照及 Begovic/Fridsma 三航速规则迎浪升沉、纵摇、相位和垂向加速度独立验证，使 Gate 2 达到 `20/20 PASS`；禁止响应反演、经验倍率、逐点拟合和选择性删点。**

## 通过条件

1. Sun 2007 `NUM` 各系数中位相对误差 `<=10%`、归一化均方根误差 `<=15%`，数值收敛和物理趋势通过，并明确标记为同源数值复现。
2. Begovic 三航速升沉/纵摇中位误差 `<=15%`、归一化均方根误差 `<=20%`，主峰误差 `<=10%`，相位误差 `<=15 deg`。
3. Fridsma 重心垂向加速度归一化均方根误差 `<=25%`。
4. 频域--时域一致、线性缩放、点加速度运动学、数据追溯、数值诊断全部通过。
5. 一键复现全部数据、图表、三维视图、动态可视化和机器验收；Gate 1 与完整测试无退化，Gate 2 明确为 `20/20 PASS`。

本阶段未通过前，不进入不规则波、斜浪完整六自由度、多体船或水翼模型的正式验收。

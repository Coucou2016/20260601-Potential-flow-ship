# Gate 2：14.5°二次高模态修正与空间收敛审计

日期：2026-08-27  
结论状态：**14.5°空间数值收敛通过；无冻结物理参考；二维先决门保持 FAIL，Gate 2 保持 17/20 PASS**

## 1. 目的与边界

本审计从15.0°冻结源继续推进到14.5°，检验参考隔离的0.5°斜升角同伦、主体流--薄射流根闭合、两次128模态形状修正以及三层空间重构是否保持稳定。求解过程只使用离散方程残差、根缺陷、条件数、位移和曲率约束，不读取自由面、压力、载荷或整船试验参考。

14.5°没有冻结权威物理参考，因此本结果只证明数值延拓和空间稳定资格，不能替代10°、20°、30°楔形入水物理验收。

## 2. 同伦、根闭合与二次修正

从15.0°的160面板冻结源 `self_similar_wedge_15deg_n160_dct128_rootpreserve_fixedbranch_curvature_trust025_v504` 出发，以楔顶为中心施加向远场衰减的 `-0.5 deg` 局部旋转。尺度1.0候选保持匹配面零位移，初始 `K=6.246821e-2`、根部相对失配 `2.017237e-3`、条件数比 `0.999175`，且未使用参考数据。

32模态 `close` 首步将 `K` 降至 `5.795018e-2`。沿冻结方向继续计算时接受24步，最终 `K=5.669386e-3`、根部相对失配 `9.961795e-6`、条件数 `9.337491e4`。第一次128模态 `preserve` 修正将160面板源的 `K` 降至 `4.452140e-3`，但其320、640和1280面板重构值分别为 `1.891268e-3`、`1.344462e-3` 和 `1.257046e-3`；后两层均未达到绝对门槛 `K<=1e-3`，因此该组结果被保留为失败证据，没有判定通过。

第二次128模态 `preserve` 修正通过完整非线性候选检查，将160面板源的 `K` 进一步降至 `4.080200e-3`，根部相对失配为 `7.440433e-7`，条件数为 `9.329390e4`。候选曲率能量比为 `0.995604`、最大位移信赖域比为 `0.247113`，满足冻结约束。

## 3. 三层空间结果

从第二次修正后的同一个160面板冻结源独立生成320、640和1280面板网格，统一使用256个射流边界积分面板且不增加伪时间迭代：

| 外自由面面板数 | 精确运动学积分 K | 根部相对失配 | 边界积分条件数 |
| ---: | ---: | ---: | ---: |
| 320 | `1.480256e-3` | `1.963348e-4` | `1.930168e5` |
| 640 | `9.207826e-4` | `2.740806e-4` | `4.320012e5` |
| 1280 | `8.258975e-4` | `3.089293e-4` | `1.038070e6` |

320面板层高于 `K<=1e-3`，640和1280面板均通过且继续下降。最细两层之间，偶极系数变化 `0.021481%`、积分垂向力变化 `0.000923%`、压力峰变化 `0.001037%`、自由面相对二范数变化 `0.795479%`、完整船体压力曲线变化 `0.002289%`。三层趋势、同源检查和空间稳定性机器判定均为 `PASS`。

诊断运动学积分在最细两层间变化 `10.3048%`，但两层绝对值均已低于门槛；物理输出和几何量满足冻结的5%空间稳定要求。该差异被保留在机器清单中，没有选择性删除。

最细网格检查点经哈希校验、文件读取和边界元重建后的最大相对往返误差为 `4.336809e-17`。

## 4. 冻结证据

| 证据 | 路径或状态 |
| --- | --- |
| 14.5°同伦初值 | `outputs/self_similar_wedge_14p5deg_from15_n160_rotate_homotopy0_v509` |
| 32模态根闭合 | `outputs/self_similar_wedge_14p5deg_n160_dct32_rootclose_fixedbranch_curvature_trust025_v510` |
| 根闭合方向复用 | `outputs/self_similar_wedge_14p5deg_n160_dct32_rootclose_direction_reuse32_v511` |
| 第一次128模态源 | `outputs/self_similar_wedge_14p5deg_n160_dct128_rootpreserve_fixedbranch_curvature_trust025_v512` |
| 第一次三层网格失败证据 | `outputs/self_similar_wedge_14p5deg_dct128_grid_convergence_v516/grid_convergence.json` |
| 第二次128模态冻结源 | `outputs/self_similar_wedge_14p5deg_n160_dct128_second_rootpreserve_fixedbranch_curvature_trust025_v517` |
| 最终320/640/1280面板层级 | `outputs/self_similar_wedge_14p5deg_n320_jet256_dct128_second_regrid0_v518`、`v519`、`v520` |
| 最终空间收敛机器清单 | `outputs/self_similar_wedge_14p5deg_dct128_second_grid_convergence_v521/grid_convergence.json` |
| 空间数值状态 | `PASS` |
| 独立物理验证状态 | `FAIL` |

## 5. 结论与下一步

14.5°已形成参考隔离、根接口闭合、三层空间稳定且可无损重载的数值同伦源。第一次高模态修正后的绝对阈值失败已完整保留，第二次修正才满足640和1280面板 `K<=1e-3` 的硬条件。

该结果支持继续按0.5°步长向14.0°推进，但不改变二维先决门和Gate 2状态。到达10°后，仍必须完成冻结参考自由面、压力、积分载荷、时间步、远域及独立路径复现验收；只有物理和数值条件全部通过，才能将10°子门改判为 `PASS`。

本轮未修改生产代码。最新完整软件回归仍为 `602 passed, 0 failed, 1247 warnings`；代码未变化，因此未重复运行完整回归。

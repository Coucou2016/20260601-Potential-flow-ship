# Gate 2：17.5°根闭合与空间收敛审计

日期：2026-08-26  
结论状态：**17.5°空间数值收敛通过；无冻结物理参考；二维先决门保持 FAIL，Gate 2保持17/20 PASS**

## 1. 目的与证据边界

本审计从18.0°冻结数值源继续向17.5°推进，检验0.5°低斜升角同伦、主体流--薄射流根闭合、128模态形状修正和三层空间重构是否保持稳定。求解过程未读取自由面、压力、载荷或整船试验参考，所有候选只由方程残差、根缺陷、条件数、位移和曲率约束裁决。

17.5°没有冻结的权威物理参考，因此本结果只具有数值延拓资格，不能替代10°、20°、30°楔形入水物理验收。

## 2. 同伦、根闭合与高模态修正

从18.0°的160面板冻结源 `self_similar_wedge_18deg_n160_dct128_rootpreserve_fixedbranch_curvature_trust025_v445` 出发，以楔顶为中心施加向远场衰减的 `-0.5 deg` 局部旋转。尺度1.0候选保持匹配面零位移，初始 `K=6.152997e-2`、根部相对失配 `2.139483e-3`、条件数比 `0.999267`。

32模态 `close` 约束的首步将 `K` 降至 `5.690435e-2`。沿冻结方向继续计算时接受23步，最终 `K=4.588123e-3`，根部相对失配降至 `4.750280e-6`，条件数降至 `9.698191e4`。随后128模态 `preserve` 修正通过完整非线性候选检查，将160面板源的 `K` 降至 `3.810725e-3`，条件数为 `9.693236e4`。

## 3. 三层空间结果

从同一160面板冻结源独立生成320、640和1280面板网格，统一使用256个射流边界积分面板且不增加伪时间迭代：

| 外自由面面板数 | 精确运动学积分 K | 根部相对失配 | 边界积分条件数 |
| ---: | ---: | ---: | ---: |
| 320 | `1.231256e-3` | `1.952572e-4` | `2.005003e5` |
| 640 | `6.343895e-4` | `2.714809e-4` | `4.488819e5` |
| 1280 | `5.250788e-4` | `3.057803e-4` | `1.079014e6` |

320面板层略高于 `K<=1e-3`，但640和1280面板均通过且继续下降。最细两层之间，偶极系数变化 `0.021978%`、积分垂向力变化 `0.000953%`、压力峰变化 `0.000976%`、自由面相对二范数变化 `0.857735%`、完整船体压力曲线变化 `0.002280%`。三层趋势、同源检查和空间稳定性机器判定均为 `PASS`。

最细检查点经哈希校验、文件读取和边界元重建后的最大相对往返误差为 `8.847090e-17`。

## 4. 冻结证据

| 证据 | 路径或状态 |
| --- | --- |
| 17.5°同伦初值 | `outputs/self_similar_wedge_17p5deg_from18_n160_rotate_homotopy0_v451` |
| 32模态根闭合 | `outputs/self_similar_wedge_17p5deg_n160_dct32_rootclose_fixedbranch_curvature_trust025_v452` |
| 根闭合方向复用 | `outputs/self_similar_wedge_17p5deg_n160_dct32_rootclose_direction_reuse32_v453` |
| 128模态冻结源 | `outputs/self_similar_wedge_17p5deg_n160_dct128_rootpreserve_fixedbranch_curvature_trust025_v454` |
| 320/640/1280面板层级 | `outputs/self_similar_wedge_17p5deg_n320_jet256_dct128_regrid0_v455`、`v456`、`v457` |
| 空间收敛机器清单 | `outputs/self_similar_wedge_17p5deg_dct128_grid_convergence_v458/grid_convergence.json` |
| 空间数值状态 | `PASS` |
| 独立物理验证状态 | `FAIL` |

## 5. 结论与下一步

17.5°已形成参考隔离、根接口闭合、三层空间稳定且可无损重载的数值同伦源。结果支持继续按0.5°步长向17.0°推进，但不改变二维先决门和Gate 2状态。

到达10°后，仍必须另行完成冻结参考自由面、压力、积分载荷、时间步、远域和独立路径复现验收；只有这些物理与数值条件同时通过，才能将10°子门改判为 `PASS`。

本轮未修改生产代码。最新完整软件回归仍为 `602 passed, 0 failed, 1247 warnings`；代码未变化，因此没有重复运行完整回归。

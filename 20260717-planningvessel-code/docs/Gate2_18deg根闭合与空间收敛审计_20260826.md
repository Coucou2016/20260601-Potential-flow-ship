# Gate 2：18.0°根闭合与空间收敛审计

日期：2026-08-26  
结论状态：**18.0°空间数值收敛通过；无冻结物理参考；二维先决门保持 FAIL，Gate 2 保持 17/20 PASS**

## 1. 审计目的与边界

本审计检验18.5°到18.0°低斜升角同伦后，主体流--薄射流接口能否闭合，以及精确端点运动学积分能否随空间加密稳定下降。求解过程只使用控制方程残差、根接口缺陷、条件数、位移和曲率约束，不读取自由面、压力、载荷或整船试验参考。

因此，本结果只能证明冻结离散方程上的空间稳定性，并为继续向10°推进提供数值初值；它不能代替10°、20°、30°楔形入水的独立物理验证。

## 2. 参考隔离同伦与根闭合

从18.5°冻结源

`self_similar_wedge_18p5deg_n160_dct128_rootpreserve_fixedbranch_curvature_trust025_v435`

出发，以楔顶为中心施加向远场衰减的 `-0.5 deg` 局部旋转。尺度1.0候选保持原匹配面，初始精确运动学积分为 `K=6.265767e-2`，根部相对失配为 `2.112629e-3`，条件数比为 `0.999243`；求解期间未使用参考数据。

第一次32模态 `close` 约束将 `K` 降至 `5.390658e-2`。沿冻结方向继续计算时接受23步，`K` 降至 `5.387364e-3`，根部相对失配降至约 `1.80e-5`。重新线性化后的32模态方向只接受一个有效复用步，最终停在 `K=5.196300e-3`，表明该低维方向已饱和。

随后采用128模态离散余弦基，并用 `preserve` 约束保持已经闭合的根接口。候选通过完整非线性重建、固定匹配面、最大位移和曲率能量检查，将160面板源的 `K` 降至 `3.583458e-3`；最终边界积分条件数为 `9.750600e4`。

## 3. 三层空间重构

从同一个160面板冻结源独立重构320、640和1280面板网格，统一使用256个射流边界积分面板，不增加伪时间迭代：

| 外自由面面板数 | 精确运动学积分 K | 根部相对失配 | 边界积分条件数 |
| ---: | ---: | ---: | ---: |
| 320 | `9.815987e-4` | `1.718261e-4` | `2.016712e5` |
| 640 | `3.716591e-4` | `2.484567e-4` | `4.515073e5` |
| 1280 | `2.554579e-4` | `2.826248e-4` | `1.085376e6` |

三层均满足数值门槛 `K<=1e-3`。640到1280面板之间，偶极系数相对变化为 `0.021993%`，积分垂向力变化为 `0.000958%`，压力峰变化为 `0.000974%`，自由面相对二范数变化为 `0.367665%`，完整船体压力曲线变化为 `0.002298%`。统一空间收敛清单判定为 `PASS`。

最细网格检查点经哈希检查、文件读取和边界元重建后的最大相对往返误差为 `1.125657e-16`，证明冻结状态可无损复现。

## 4. 冻结证据

| 证据 | 路径或状态 |
| --- | --- |
| 18.0°同伦初值 | `outputs/self_similar_wedge_18deg_from18p5_n160_rotate_homotopy0_v440` |
| 第一次32模态根闭合 | `outputs/self_similar_wedge_18deg_n160_dct32_rootclose_fixedbranch_curvature_trust025_v441` |
| 第一次方向复用 | `outputs/self_similar_wedge_18deg_n160_dct32_rootclose_direction_reuse32_v442` |
| 重新线性化与方向复用 | `outputs/self_similar_wedge_18deg_n160_dct32_rootclose_relinearized_direction_reuse32_v444` |
| 128模态冻结源 | `outputs/self_similar_wedge_18deg_n160_dct128_rootpreserve_fixedbranch_curvature_trust025_v445` |
| 320/640/1280面板层级 | `outputs/self_similar_wedge_18deg_n320_jet256_dct128_regrid0_v446`、`v447`、`v448` |
| 空间收敛机器清单 | `outputs/self_similar_wedge_18deg_dct128_grid_convergence_v450/grid_convergence.json` |
| 空间数值状态 | `PASS` |
| 独立物理验证状态 | `FAIL` |

## 5. 结论与下一步

18.0°已形成参考隔离、根接口受控、三层空间稳定且可无损重载的数值同伦源。该结果扩展了低斜升角稳定分支，但没有新增18.0°权威物理参考，故二维楔形入水先决门仍为 `FAIL`，Gate 2仍为 `17/20 PASS`。

下一最小任务是继续按0.5°步长向17.5°推进，并在每一角度重复“局部旋转--根闭合--高模态修正--三层重构”。到达10°后，必须另行完成冻结参考曲线、压力、积分载荷、时间步、远域和独立路径复现验收，才能将10°物理子门改判为 `PASS`。

本轮未修改生产代码。最新完整软件回归仍为 `602 passed, 0 failed, 1247 warnings`；该回归早于本轮新增输出和文档，但代码未发生变化。

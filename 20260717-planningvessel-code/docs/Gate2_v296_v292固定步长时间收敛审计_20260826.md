# Gate 2 v296：v292 固定步长时间收敛审计

日期：2026-08-26  
状态：**时间步收敛子项 `PASS`；二维楔形入水先决门仍为 `FAIL`，Gate 2 仍为 `17/20 PASS`。**

## 1. 审计目的

`v292` 是 20°、`R=30,N=600` 的正式冻结候选，其精确运动学积分为 `K=0.000810506`。由于该值已经低于求解器默认停止阈值，普通恢复会在零步处退出，无法检验更细伪时间步是否改变几何、压力和积分力。本审计新增仅用于诊断的显式固定步模式；生产默认停止逻辑保持不变。

三档计算必须来自同一个冻结检查点，只改变伪 Courant--Friedrichs--Lewy 数和步数，并用 `1/2/4` 步匹配相同新增伪时间。参考自由面和压力只在推进结束后评分，不参与求解与停止。

## 2. 冻结输入

- 冻结检查点：`v292`。
- 检查点 `SHA-256`：`6d02115ed1c66d36e626a62da3b23ebf6773fcb38e6a94117f9088abc23fad30`。
- 三档 `CFL`：`0.25/0.125/0.0625`。
- 三档步数：`1/2/4`。
- 根部内迭代：每步 `8` 次。
- 三个恢复任务均逐项验证源文件哈希；检查点重建相对误差除历史运动学均方根的浮点舍入量 `9.80e-17` 外均为零。

## 3. 末态结果

| 指标 | `CFL=0.25` | `CFL=0.125` | `CFL=0.0625` |
| --- | ---: | ---: | ---: |
| 累计伪时间 | 0.000185574486 | 0.000185575902 | 0.000185576610 |
| 运动学均方根 | 0.004933889 | 0.004932428 | 0.004933472 |
| 精确运动学积分 `K` | 0.000779672 | 0.000779080 | 0.000779433 |
| 压力峰值 | 17.7828759 | 17.7828616 | 17.7828688 |
| 积分垂向力 | 42.6093043 | 42.6086588 | 42.6089804 |
| 偶极系数 | 8.14484545 | 8.14483902 | 8.14484240 |

累计伪时间的三档相对离散为 `1.1445e-5`，低于 `0.5%` 限值。最细两级的末态变化中，运动学均方根为 `0.0212%`，精确 `K` 为 `0.0453%`，压力峰值为 `0.0000407%`，积分垂向力为 `0.000755%`。整条运动学轨迹、自由面曲线和压力曲线的相对二范数差也分别只有 `0.0216%`、`0.00000265%` 和 `0.00208%`，全部低于预先声明的 `5%` 限值。

## 4. 判定边界

本审计证明：在 `v292` 的冻结网格和冻结物理状态附近，当前显式伪时间离散已经稳定，20° 候选的剩余误差不能归因于步长过粗。它不证明自由面参考数据一致，也不代替空间、远域或三角度验收。

当前仍有三项硬阻挡：

1. `R=20` 与 `R=30` 的精确 `K` 相对变化为 `6.535%`，高于 `5%`。
2. Zhao 自由面归一化均方根误差为 `10.84%`，高于冻结的 `5%` 门槛；Iafrati 对照虽通过，但两套参考必须同时报告。
3. `10/30 deg` 尚未按同一冻结算法完成网格、远域、时间步、压力和积分力验收。

因此机器审计输出为 `time_step_convergence_only=PASS`、`physical_validation_status=FAIL`。证据文件为 `outputs/self_similar_wedge_20deg_n600_far30_v292_time_step_convergence_v296/time_step_convergence.json`，其 `SHA-256` 为 `c322c93f6e885b93271c6a07bb0de11b28bec0bb82757b3247207d84717eae2b`。

## 5. 软件验证

固定步模式保持求解器默认行为不变；只有显式传入诊断开关时才强制推进指定步数。网格/时间收敛、求解器、检查点和信赖域相关的定向回归为 `70 passed, 0 failed`；完整软件回归为 `576 passed, 0 failed, 1247 warnings`，耗时 `788.79 s`。警告来自既有验证统计空切片，另有 `pytest-asyncio` 配置弃用提示，不改变本审计的数值判定。

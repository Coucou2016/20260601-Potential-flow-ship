# Gate 2：16.5°二次高模态修正与空间收敛审计

日期：2026-08-26  
结论状态：**16.5°空间数值收敛通过；无冻结物理参考；二维先决门保持 FAIL，Gate 2保持17/20 PASS**

## 1. 目的与边界

本审计从17.0°冻结源推进到16.5°，检验低斜升角继续下降时，参考隔离同伦、主体流--薄射流根闭合、高模态形状修正和三层空间重构能否保持稳定。求解过程只使用离散方程残差、根缺陷、条件数、位移和曲率约束，不读取自由面、压力、载荷或整船试验参考。

16.5°没有冻结权威物理参考，因此本结果只证明数值延拓和空间稳定资格，不能替代10°、20°、30°楔形入水物理验收。

## 2. 同伦与根闭合

从17.0°的160面板冻结源 `self_similar_wedge_17deg_n160_dct128_rootpreserve_fixedbranch_curvature_trust025_v462` 出发，以楔顶为中心施加向远场衰减的 `-0.5 deg` 局部旋转。尺度1.0候选保持匹配面零位移，初始 `K=6.328051e-2`、根部相对失配 `2.050962e-3`、条件数比 `0.999140`，且未使用参考数据。

32模态 `close` 首步将 `K` 降至 `5.455452e-2`。沿冻结方向继续计算时接受23步，最终 `K=6.232027e-3`、根部相对失配 `1.671289e-5`、条件数 `9.578894e4`。

## 3. 第一次高模态源及失败边界

第一次128模态 `preserve` 修正将160面板源的 `K` 降至 `4.505123e-3`。从该源生成的网格结果为：

| 外自由面面板数 | 精确运动学积分 K | 根部相对失配 |
| ---: | ---: | ---: |
| 320 | `1.962204e-3` | `2.066351e-4` |
| 640 | `1.390048e-3` | `2.831928e-4` |
| 1280 | `1.293060e-3` | `3.173870e-4` |

三层物理输出已经空间稳定，最细两层自由面相对二范数变化为 `1.679319%`，但640和1280面板的绝对 `K` 均高于 `1e-3`。因此第一次候选不具备数值通过资格，不能因其他输出稳定而放宽绝对门槛。

## 4. 第二次128模态重线性化

在第一次高模态修正后的新几何上重新计算完整128模态雅可比，仍采用固定匹配面、根保持、曲率能量比不超过1.1的冻结口径。第二个非线性候选被接受，将N160的 `K` 从 `4.505123e-3` 降至 `3.788880e-3`，降低约15.9%，条件数降至 `9.570771e4`。

从第二个冻结源独立重构三层网格：

| 外自由面面板数 | 精确运动学积分 K | 根部相对失配 | 边界积分条件数 |
| ---: | ---: | ---: | ---: |
| 320 | `1.194858e-3` | `1.980372e-4` | `1.979675e5` |
| 640 | `6.044345e-4` | `2.749621e-4` | `4.431667e5` |
| 1280 | `4.979733e-4` | `3.095034e-4` | `1.065150e6` |

640和1280面板均满足 `K<=1e-3`。最细两层之间，偶极系数变化 `0.021787%`、积分垂向力变化 `0.000942%`、压力峰变化 `0.001000%`、自由面相对二范数变化 `0.942575%`、完整船体压力曲线变化 `0.002292%`。三层趋势、同源检查和空间稳定性机器判定均为 `PASS`。

最细网格检查点经哈希校验、文件读取和边界元重建后的最大相对往返误差为 `1.119330e-16`。

## 5. 冻结证据

| 证据 | 路径或状态 |
| --- | --- |
| 16.5°同伦初值 | `outputs/self_similar_wedge_16p5deg_from17_n160_rotate_homotopy0_v467` |
| 32模态根闭合与方向复用 | `outputs/self_similar_wedge_16p5deg_n160_dct32_rootclose_fixedbranch_curvature_trust025_v468`、`v469` |
| 第一次128模态源 | `outputs/self_similar_wedge_16p5deg_n160_dct128_rootpreserve_fixedbranch_curvature_trust025_v470` |
| 第一次失败候选网格与清单 | `outputs/self_similar_wedge_16p5deg_n320_jet256_dct128_regrid0_v471`至`v473`；`self_similar_wedge_16p5deg_dct128_grid_candidate_v474` |
| 第二次128模态冻结源 | `outputs/self_similar_wedge_16p5deg_n160_dct128_second_rootpreserve_fixedbranch_curvature_trust025_v475` |
| 最终320/640/1280面板层级 | `outputs/self_similar_wedge_16p5deg_n320_jet256_dct128_second_regrid0_v476`、`v477`、`v478` |
| 最终空间收敛机器清单 | `outputs/self_similar_wedge_16p5deg_dct128_second_grid_convergence_v479/grid_convergence.json` |
| 空间数值状态 | `PASS` |
| 独立物理验证状态 | `FAIL` |

## 6. 结论与下一步

16.5°在第一次高模态源上未达到绝对运动学积分门槛；重新线性化第二个128模态方向后，形成参考隔离、根接口闭合、三层空间稳定且可无损重载的数值源。失败候选被完整保留，没有放宽阈值或删除不利层级。

该结果支持继续向16.0°推进，但不改变二维先决门和Gate 2状态。到达10°后，仍必须完成冻结参考自由面、压力、积分载荷、时间步、远域和独立路径复现验收。

本轮未修改生产代码。最新完整软件回归仍为 `602 passed, 0 failed, 1247 warnings`；代码未变化，因此未重复运行完整回归。

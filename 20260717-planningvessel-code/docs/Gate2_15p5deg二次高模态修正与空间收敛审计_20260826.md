# Gate 2：15.5°二次高模态修正与空间收敛审计

日期：2026-08-26  
结论状态：**15.5°空间数值收敛通过；无冻结物理参考；二维先决门保持 FAIL，Gate 2保持17/20 PASS**

## 1. 目的与边界

本审计从16.0°冻结源推进到15.5°，检验低斜升角继续下降时，参考隔离同伦、主体流--薄射流根闭合、高模态形状修正和三层空间重构能否保持稳定。求解过程只使用离散方程残差、根缺陷、条件数、位移和曲率约束，不读取自由面、压力、载荷或整船试验参考。

15.5°没有冻结权威物理参考，因此本结果只证明数值延拓和空间稳定资格，不能替代10°、20°、30°楔形入水物理验收。

## 2. 同伦与根闭合

从16.0°的160面板冻结源 `self_similar_wedge_16deg_n160_dct128_rootpreserve_fixedbranch_curvature_trust025_v483` 出发，以楔顶为中心施加向远场衰减的 `-0.5 deg` 局部旋转。尺度1.0候选保持匹配面零位移，初始 `K=6.249348e-2`、根部相对失配 `2.040259e-3`、条件数比 `0.999295`，且未使用参考数据。

32模态 `close` 首步将 `K` 降至 `5.371597e-2`。沿冻结方向继续计算时接受23步，最终 `K=5.842972e-3`、根部相对失配 `3.534877e-5`、条件数 `9.457521e4`。第一次128模态 `preserve` 修正将160面板源的 `K` 降至 `4.332751e-3`，根部相对失配为 `3.214659e-5`，条件数为 `9.452396e4`。

## 3. 第一次高模态源的失败证据

第一次128模态源生成的三层网格如下：

| 外自由面面板数 | 精确运动学积分 K | 根部相对失配 | 边界积分条件数 |
| ---: | ---: | ---: | ---: |
| 320 | `1.772794e-3` | `2.267911e-4` | `1.955651e5` |
| 640 | `1.209001e-3` | `3.037138e-4` | `4.377554e5` |
| 1280 | `1.115152e-3` | `3.384851e-4` | `1.052029e6` |

该候选的自由面、压力和载荷空间变化满足5%稳定阈值，空间收敛脚本状态为 `PASS`；但640和1280面板的绝对运动学积分均高于冻结门槛 `K<=1e-3`。因此该候选被明确判为数值阈值失败，不能用“物理输出看似稳定”替代绝对残差要求，也没有删除不利网格层级。

## 4. 第二次高模态修正与最终空间结果

在第一次128模态更新后的几何上重新构造128模态雅可比。第二次 `preserve` 修正通过完整非线性候选检查，将160面板源的 `K` 从 `4.332751e-3` 降至 `3.893837e-3`，根部相对失配降至 `1.992464e-5`，条件数为 `9.448014e4`。

从第二次修正后的同一个160面板冻结源独立生成320、640和1280面板网格，统一使用256个射流边界积分面板且不增加伪时间迭代：

| 外自由面面板数 | 精确运动学积分 K | 根部相对失配 | 边界积分条件数 |
| ---: | ---: | ---: | ---: |
| 320 | `1.289937e-3` | `2.161099e-4` | `1.954665e5` |
| 640 | `7.109442e-4` | `2.936113e-4` | `4.375249e5` |
| 1280 | `6.085306e-4` | `3.283860e-4` | `1.051464e6` |

320面板层高于 `K<=1e-3`，640和1280面板均通过且继续下降。最细两层之间，偶极系数变化 `0.021610%`、积分垂向力变化 `0.000933%`、压力峰变化 `0.001021%`、自由面相对二范数变化 `1.138551%`、完整船体压力曲线变化 `0.002290%`。三层趋势、同源检查和空间稳定性机器判定均为 `PASS`。

诊断运动学积分在最细两层间仍变化 `14.4053%`，但两层绝对值均已低于门槛；物理输出和几何量满足冻结的5%空间稳定要求。该差异被保留在机器清单中，没有选择性删除。

最细网格检查点经哈希校验、文件读取和边界元重建后的最大相对往返误差为 `1.214306e-17`。

## 5. 冻结证据

| 证据 | 路径或状态 |
| --- | --- |
| 15.5°同伦初值 | `outputs/self_similar_wedge_15p5deg_from16_n160_rotate_homotopy0_v488` |
| 32模态根闭合与方向复用 | `outputs/self_similar_wedge_15p5deg_n160_dct32_rootclose_fixedbranch_curvature_trust025_v489`、`v490` |
| 第一次128模态源 | `outputs/self_similar_wedge_15p5deg_n160_dct128_rootpreserve_fixedbranch_curvature_trust025_v491` |
| 第一次失败候选网格与清单 | `outputs/self_similar_wedge_15p5deg_n320_jet256_dct128_regrid0_v492`至`v494`；`self_similar_wedge_15p5deg_dct128_grid_candidate_v495` |
| 第二次128模态冻结源 | `outputs/self_similar_wedge_15p5deg_n160_dct128_second_rootpreserve_fixedbranch_curvature_trust025_v496` |
| 最终320/640/1280面板层级 | `outputs/self_similar_wedge_15p5deg_n320_jet256_dct128_second_regrid0_v497`、`v498`、`v499` |
| 最终空间收敛机器清单 | `outputs/self_similar_wedge_15p5deg_dct128_second_grid_convergence_v500/grid_convergence.json` |
| 空间数值状态 | `PASS` |
| 独立物理验证状态 | `FAIL` |

## 6. 结论与下一步

15.5°在第一次高模态源上未达到绝对运动学积分门槛；重新线性化第二个128模态方向后，形成参考隔离、根接口闭合、三层空间稳定且可无损重载的数值源。失败候选被完整保留，没有放宽阈值或删除不利层级。

该结果支持继续向15.0°推进，但不改变二维先决门和Gate 2状态。到达10°后，仍必须完成冻结参考自由面、压力、积分载荷、时间步、远域和独立路径复现验收。

本轮未修改生产代码。最新完整软件回归仍为 `602 passed, 0 failed, 1247 warnings`；代码未变化，因此未重复运行完整回归。
